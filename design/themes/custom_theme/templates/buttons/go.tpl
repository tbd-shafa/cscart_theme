<button title="{$alt}" class="site-btn" type="submit">{if $but_text}{$but_text}{else}{include_ext file="common/icon.tpl" class=""}{/if}</button>
<input type="hidden" name="dispatch" value="{$but_name}" />
